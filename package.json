{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "@deck.gl/core": "^8.5.9",
    "@deck.gl/layers": "^8.5.9",
    "@deck.gl/react": "^8.5.9",
    "@loaders.gl/core": "^3.0.12",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "baseui": "^10.2.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "styletron-engine-monolithic": "0.0.5",
    "styletron-react": "^6.0.1",
    "web-vitals": "^1.0.1",
    "web3": "^1.5.3"
  },
  "scripts": {
    "deploy": "yarn build && rm -rf docs && mv build docs && echo 'planeteth.app' >> ./docs/CNAME && git add --all && git commit --message 'Publish' && git push",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
